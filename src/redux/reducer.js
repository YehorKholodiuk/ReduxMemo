const initialState = {
    cards: [
        {
            title: 'First todo',
            done: true,
        }, {
            title: 'Second todo',
            done: false,
        }
    ]
};

const kanban = (state = initialState, action) => {
    return kanban
}

export default kanban